import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
test("every declared command has a dispatcher case or dedicated bootstrap handler",()=>{const protocol=fs.readFileSync(new URL("../src/shared/protocol.ts",import.meta.url),"utf8");const block=protocol.match(/export type BrowserCommandType\s*=([\s\S]*?);/)?.[1]??"";const commands=[...block.matchAll(/"([^"]+)"/g)].map(m=>m[1]);const dispatcher=fs.readFileSync(new URL("../src/extension/runtime/RuntimeDispatcher.ts",import.meta.url),"utf8");const handled=new Set([...dispatcher.matchAll(/case"([^"]+)"/g)].map(m=>m[1]));for(const c of ["runtime_ping","runtime_list_browsers"])handled.add(c);assert.deepEqual(commands.filter(c=>!handled.has(c)),[]);});
