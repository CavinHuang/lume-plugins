services:
  mirror:
    build:
      context: .
      dockerfile: service/Dockerfile
    restart: unless-stopped
    expose:
      - "8787"
    volumes:
      - mirror-data:/data

volumes:
  mirror-data:
